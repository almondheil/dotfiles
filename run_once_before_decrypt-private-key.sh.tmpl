#!/bin/sh

if [ ! -f "/home/almond/.config/chezmoi/key.txt" ]; then
    mkdir -p "/home/almond/.config/chezmoi"
    chezmoi age decrypt --output "/home/almond/.config/chezmoi/key.txt" --passphrase "{{ .chezmoi.sourceDir }}/key.txt.age"
    chmod 600 "/home/almond/.config/chezmoi/key.txt"
fi
